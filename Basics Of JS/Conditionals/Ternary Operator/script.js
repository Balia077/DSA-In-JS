// Ternary Operator
// Syntax: condition ? expr1 : expr2

console.log(112 > 13 ? "112 is greater" : "13 is greater");

// Nested Ternary Operator

let num = 0;
console.log(num > 0 ? "Positive" : num < 0 ? "Negative" : "Zero");
// Output: Zero